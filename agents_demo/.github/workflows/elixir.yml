# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
#
# https://github.com/erlef/setup-beam/tree/v1.18.2

name: Elixir CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  MIX_ENV: test
  OPENAI_API_KEY: invalid
  ANTHROPIC_API_KEY: invalid
  GOOGLE_API_KEY: invalid
  AWS_ACCESS_KEY_ID: invalid
  AWS_SECRET_ACCESS_KEY: invalid
  AWS_REGION: invalid
  PERPLEXITY_API_KEY: invalid
  MISTRAL_API_KEY: invalid
  VERTEX_API_KEY: invalid
  VERTEX_API_ENDPOINT: invalid

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      db:
        # Matches the postgres:17-alpine in docker-compose.yml, so a
        # version-specific failure cannot pass locally and fail here.
        image: postgres:17
        ports: ['5432:5432']
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    strategy:
      fail-fast: false
      matrix:
        include:
          # Only 1.19+: :anu, the WhatsApp channel SDK, requires ~> 1.19, and
          # mix.exs was raised to match. The former 1.17 leg cannot resolve.
          - pair:
              elixir: "1.19"
              otp: "27"
            lint: lint

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1.24.0
        with:
          elixir-version: ${{ matrix.pair.elixir }}
          otp-version: ${{ matrix.pair.otp }}

      - uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - run: mix deps.get

      - run: mix format --check-formatted
        if: ${{ matrix.lint }}

      - run: mix deps.get && mix deps.unlock --check-unused
        if: ${{ matrix.lint }}

      - run: mix deps.compile

      - run: mix compile --warnings-as-errors
        if: ${{ matrix.lint }}

      - name: Run credo
        if: ${{ matrix.lint }}
        run: mix credo --format=oneline

      - name: Set up database
        run: mix ecto.create && mix ecto.migrate

      - name: Run tests
        run: mix test
