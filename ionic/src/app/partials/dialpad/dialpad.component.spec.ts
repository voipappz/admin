import { DialpadComponent } from './dialpad.component';

// Thin smoke test — verifies the class loads.
describe('DialpadComponent', () => {
  it('should be defined', () => {
    expect(DialpadComponent).toBeDefined();
  });
});
