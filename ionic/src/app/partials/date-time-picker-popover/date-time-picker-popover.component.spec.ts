import { DateTimePickerPopoverComponent } from './date-time-picker-popover.component';

// Thin smoke test — verifies the class loads.
describe('DateTimePickerPopoverComponent', () => {
  it('should be defined', () => {
    expect(DateTimePickerPopoverComponent).toBeDefined();
  });
});
