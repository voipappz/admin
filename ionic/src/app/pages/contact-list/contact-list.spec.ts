import { ContactListPage } from './contact-list';

// Thin smoke test — verifies the class loads.
describe('ContactListPage', () => {
  it('should be defined', () => {
    expect(ContactListPage).toBeDefined();
  });
});
