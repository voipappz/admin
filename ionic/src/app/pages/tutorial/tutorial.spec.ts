import { TutorialPage } from './tutorial';

// Thin smoke test — verifies the class loads.
describe('TutorialPage', () => {
  it('should be defined', () => {
    expect(TutorialPage).toBeDefined();
  });
});
